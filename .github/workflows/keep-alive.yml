name: Keep Render Alive

on:
  schedule:
    # Chạy mỗi 10 phút
    - cron: '*/10 * * * *'
  workflow_dispatch: # Cho phép chạy thủ công

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Health Endpoint
        run: |
          # URL Render của bạn
          RENDER_URL="${RENDER_URL:-https://backend-web-bds.onrender.com}"
          echo "🚀 Pinging Render to keep it alive..."
          echo "URL: $RENDER_URL/health"
          curl -f "$RENDER_URL/health" && echo "✅ Success!" || echo "⚠️ Failed but continuing..."
        env:
          RENDER_URL: ${{ secrets.RENDER_URL || 'https://backend-web-bds.onrender.com' }}

